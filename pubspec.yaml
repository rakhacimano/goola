name: goola
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^5.1.0
  flutter_staggered_grid_view: ^0.7.0
  iconsax: ^0.0.8
  intl: 0.18.1
  path: ^1.9.0
  sqflite: ^2.3.3
  d_chart: ^2.6.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
